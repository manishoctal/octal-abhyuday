<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">#FF7A00</item>
        <item name="colorPrimaryDark">#0F1035</item>
        <item name="colorAccent">#FF4F87</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>

    <!--
        Launch/splash theme. Parent is Theme.SplashScreen (from androidx.core:core-splashscreen).
        - android:background   → used on pre-Android-12 as the activity background while loading
        - windowSplashScreenBackground     → Android 12+ splash background color
        - windowSplashScreenAnimatedIcon   → Android 12+ center icon on splash
        - postSplashScreenTheme            → theme applied after splash dismisses (required)
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">#FFFFFF</item>
        <item name="windowSplashScreenBackground">#FFFFFF</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>

</resources>
